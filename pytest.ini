[pytest]
; 目录约定：用例都在 testcases 下，执行 pytest 即可全量运行
testpaths = testcases

; -v 用例详情 / -ra 汇总失败原因 / --strict-markers 防止 mark 拼写错误被静默忽略
addopts = -v -ra --strict-markers --alluredir=allure-results

markers =
    defect: 记录疑似服务端缺陷、锁定当前实际行为的回归用例

; 断言失败时把响应内容打出来，方便定位
log_cli = false
